# 需求流水线评测任务集
# 一行一条，# 开头是注释。按难度分层，难题全挂是正常的——
# 任务集里没有难题，测出来的高完成率没有说服力。

# ---------- 易：单文件 / 单方法 ----------
给 StringTools 增加一个手机号脱敏方法，中间四位替换成星号，并补上单元测试
给 sendMessage 接口的 messageContent 参数增加长度校验，超过限制时返回明确的错误提示
给 UserInfo 实体和 user_info 表增加一个 lastActiveTime 字段，登录时更新它

# ---------- 中：跨层改动 ----------
会话列表支持按联系人昵称模糊搜索，后端提供接口
群聊增加"仅群主可发言"开关，非群主发言时被拒绝并提示
AI 助手增加每用户每日调用次数上限，超限时返回友好提示

好友申请列表支持按申请时间倒序分页

# ---------- 难：多表 / 新链路 ----------
实现消息撤回：发送后两分钟内可撤回，撤回后双方会话列表同步更新
群公告增加已读回执，能查看哪些成员已读
给聊天消息增加"引用回复"，回复时带上被引用消息的摘要
