node_modules/
# ffmpeg/ffprobe 是几十MB的二进制，不进版本库。
# 使用者需要自己下载，说明见 mychat-front/assets/README.md
mychat-front/assets/ffmpeg.exe
mychat-front/assets/ffprobe.exe

# 构建产物。之前 mychat-java/target 和 mychat-front/out 是被跟踪的，
# 每次编译都会往提交里塞一百多个 .class，真正的改动全被埋掉了
target/
mychat-front/out/

# 运行时产生的目录（日志、上传的头像和图片），不该进版本库。
# 必须带前导斜杠锚定到仓库根：不带斜杠的 mychat/ 会匹配任意层级的同名目录，
# 把整个 com/mychat/ 源码树也一起忽略掉——已跟踪的文件不受影响，
# 但新增的源文件会被静默吞掉，本地编译得好好的、推上去却缺文件
/logs/
/easychat/
/mychat/
