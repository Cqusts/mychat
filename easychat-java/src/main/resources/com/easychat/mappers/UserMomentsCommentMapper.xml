<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.easychat.mappers.UserMomentsCommentMapper">

    <!--实体映射-->
    <resultMap id="base_result_map" type="com.easychat.entity.po.UserMomentsComment">
        <!--评论ID-->
        <id column="comment_id" property="commentId"/>
        <!--动态ID-->
        <result column="moments_id" property="momentsId"/>
        <!--评论用户ID-->
        <result column="user_id" property="userId"/>
        <!--评论用户昵称-->
        <result column="nick_name" property="nickName"/>
        <!--回复用户ID-->
        <result column="reply_user_id" property="replyUserId"/>
        <!--回复用户昵称-->
        <result column="reply_nick_name" property="replyNickName"/>
        <!--评论内容-->
        <result column="content" property="content"/>
        <!--评论时间-->
        <result column="create_time" property="createTime"/>
        <!--状态-->
        <result column="status" property="status"/>
    </resultMap>


    <!-- 通用查询结果列-->
    <sql id="base_column_list">
        comment_id
        ,moments_id,user_id,nick_name,reply_user_id,
		 reply_nick_name,content,create_time,status
    </sql>

    <sql id="base_condition_filed">
        <if test="query.commentId != null">
            and comment_id = #{query.commentId}
        </if>
        <if test="query.momentsId != null">
            and moments_id = #{query.momentsId}
        </if>
        <if test="query.userId != null and query.userId!=''">
            and user_id = #{query.userId}
        </if>
        <if test="query.nickName != null and query.nickName!=''">
            and nick_name = #{query.nickName}
        </if>
        <if test="query.replyUserId != null and query.replyUserId!=''">
            and reply_user_id = #{query.replyUserId}
        </if>
        <if test="query.replyNickName != null and query.replyNickName!=''">
            and reply_nick_name = #{query.replyNickName}
        </if>
        <if test="query.content != null and query.content!=''">
            and content = #{query.content}
        </if>
        <if test="query.createTime != null">
            and create_time = #{query.createTime}
        </if>
        <if test="query.status != null">
            and status = #{query.status}
        </if>
    </sql>
    <!-- 通用条件列-->
    <sql id="base_condition">
        <where>
            <include refid="base_condition_filed"/>
        </where>
    </sql>

    <!-- 通用查询条件列-->
    <sql id="query_condition">
        <where>
            <include refid="base_condition_filed"/>
            <if test="query.userIdFuzzy!= null  and query.userIdFuzzy!=''">
                and user_id like concat('%', #{query.userIdFuzzy}, '%')
            </if>
            <if test="query.nickNameFuzzy!= null  and query.nickNameFuzzy!=''">
                and nick_name like concat('%', #{query.nickNameFuzzy}, '%')
            </if>
            <if test="query.replyUserIdFuzzy!= null  and query.replyUserIdFuzzy!=''">
                and reply_user_id like concat('%', #{query.replyUserIdFuzzy}, '%')
            </if>
            <if test="query.replyNickNameFuzzy!= null  and query.replyNickNameFuzzy!=''">
                and reply_nick_name like concat('%', #{query.replyNickNameFuzzy}, '%')
            </if>
            <if test="query.contentFuzzy!= null  and query.contentFuzzy!=''">
                and content like concat('%', #{query.contentFuzzy}, '%')
            </if>
        </where>
    </sql>

    <!-- 查询集合-->
    <select id="selectList" resultMap="base_result_map">
        SELECT
        <include refid="base_column_list"/>
        FROM user_moments_comment
        <include refid="query_condition"/>
        <if test="query.orderBy!=null">
            order by ${query.orderBy}
        </if>
        <if test="query.simplePage!=null">
            limit #{query.simplePage.start},#{query.simplePage.end}
        </if>
    </select>

    <!-- 查询数量-->
    <select id="selectCount" resultType="java.lang.Integer">
        SELECT count(1) FROM user_moments_comment
        <include refid="query_condition"/>
    </select>

    <!-- 插入 （匹配有值的字段）-->
    <insert id="insert" parameterType="com.easychat.entity.po.UserMomentsComment">
        <selectKey keyProperty="bean.commentId" resultType="Long" order="AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey>
        INSERT INTO user_moments_comment
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="bean.momentsId != null">
                moments_id,
            </if>
            <if test="bean.userId != null">
                user_id,
            </if>
            <if test="bean.nickName != null">
                nick_name,
            </if>
            <if test="bean.replyUserId != null">
                reply_user_id,
            </if>
            <if test="bean.replyNickName != null">
                reply_nick_name,
            </if>
            <if test="bean.content != null">
                content,
            </if>
            <if test="bean.createTime != null">
                create_time,
            </if>
            <if test="bean.status != null">
                status,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="bean.momentsId!=null">
                #{bean.momentsId},
            </if>
            <if test="bean.userId!=null">
                #{bean.userId},
            </if>
            <if test="bean.nickName!=null">
                #{bean.nickName},
            </if>
            <if test="bean.replyUserId!=null">
                #{bean.replyUserId},
            </if>
            <if test="bean.replyNickName!=null">
                #{bean.replyNickName},
            </if>
            <if test="bean.content!=null">
                #{bean.content},
            </if>
            <if test="bean.createTime!=null">
                #{bean.createTime},
            </if>
            <if test="bean.status!=null">
                #{bean.status},
            </if>
        </trim>
    </insert>

    <!-- 插入或者更新 （匹配有值的字段）-->
    <insert id="insertOrUpdate" parameterType="com.easychat.entity.po.UserMomentsComment">
        INSERT INTO user_moments_comment
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="bean.commentId != null">
                comment_id,
            </if>
            <if test="bean.momentsId != null">
                moments_id,
            </if>
            <if test="bean.userId != null">
                user_id,
            </if>
            <if test="bean.nickName != null">
                nick_name,
            </if>
            <if test="bean.replyUserId != null">
                reply_user_id,
            </if>
            <if test="bean.replyNickName != null">
                reply_nick_name,
            </if>
            <if test="bean.content != null">
                content,
            </if>
            <if test="bean.createTime != null">
                create_time,
            </if>
            <if test="bean.status != null">
                status,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="bean.commentId!=null">
                #{bean.commentId},
            </if>
            <if test="bean.momentsId!=null">
                #{bean.momentsId},
            </if>
            <if test="bean.userId!=null">
                #{bean.userId},
            </if>
            <if test="bean.nickName!=null">
                #{bean.nickName},
            </if>
            <if test="bean.replyUserId!=null">
                #{bean.replyUserId},
            </if>
            <if test="bean.replyNickName!=null">
                #{bean.replyNickName},
            </if>
            <if test="bean.content!=null">
                #{bean.content},
            </if>
            <if test="bean.createTime!=null">
                #{bean.createTime},
            </if>
            <if test="bean.status!=null">
                #{bean.status},
            </if>
        </trim>
        on DUPLICATE key update
        <trim prefix="" suffix="" suffixOverrides=",">
            <if test="bean.momentsId!=null">
                moments_id = VALUES(moments_id),
            </if>
            <if test="bean.userId!=null">
                user_id = VALUES(user_id),
            </if>
            <if test="bean.nickName!=null">
                nick_name = VALUES(nick_name),
            </if>
            <if test="bean.replyUserId!=null">
                reply_user_id = VALUES(reply_user_id),
            </if>
            <if test="bean.replyNickName!=null">
                reply_nick_name = VALUES(reply_nick_name),
            </if>
            <if test="bean.content!=null">
                content = VALUES(content),
            </if>
            <if test="bean.createTime!=null">
                create_time = VALUES(create_time),
            </if>
            <if test="bean.status!=null">
                status = VALUES(status),
            </if>
        </trim>
    </insert>

    <!-- 添加 （批量插入）-->
    <insert id="insertBatch" parameterType="com.easychat.entity.po.UserMomentsComment" useGeneratedKeys="true"
            keyProperty="commentId">
        INSERT INTO user_moments_comment(
        moments_id,
        user_id,
        nick_name,
        reply_user_id,
        reply_nick_name,
        content,
        create_time,
        status
        )values
        <foreach collection="list" item="item" separator=",">
            (
            #{item.momentsId},
            #{item.userId},
            #{item.nickName},
            #{item.replyUserId},
            #{item.replyNickName},
            #{item.content},
            #{item.createTime},
            #{item.status}
            )
        </foreach>
    </insert>

    <!-- 批量新增修改 （批量插入）-->
    <insert id="insertOrUpdateBatch" parameterType="com.easychat.entity.po.UserMomentsComment">
        INSERT INTO user_moments_comment(
        moments_id,
        user_id,
        nick_name,
        reply_user_id,
        reply_nick_name,
        content,
        create_time,
        status
        )values
        <foreach collection="list" item="item" separator=",">
            (
            #{item.momentsId},
            #{item.userId},
            #{item.nickName},
            #{item.replyUserId},
            #{item.replyNickName},
            #{item.content},
            #{item.createTime},
            #{item.status}
            )
        </foreach>
        on DUPLICATE key update
        moments_id = VALUES(moments_id),
        user_id = VALUES(user_id),
        nick_name = VALUES(nick_name),
        reply_user_id = VALUES(reply_user_id),
        reply_nick_name = VALUES(reply_nick_name),
        content = VALUES(content),
        create_time = VALUES(create_time),
        status = VALUES(status)
    </insert>

    <!--多条件修改-->
    <update id="updateByParam" parameterType="com.easychat.entity.query.UserMomentsCommentQuery">
        UPDATE user_moments_comment
        <set>
            <if test="bean.momentsId != null">
                moments_id = #{bean.momentsId},
            </if>
            <if test="bean.userId != null">
                user_id = #{bean.userId},
            </if>
            <if test="bean.nickName != null">
                nick_name = #{bean.nickName},
            </if>
            <if test="bean.replyUserId != null">
                reply_user_id = #{bean.replyUserId},
            </if>
            <if test="bean.replyNickName != null">
                reply_nick_name = #{bean.replyNickName},
            </if>
            <if test="bean.content != null">
                content = #{bean.content},
            </if>
            <if test="bean.createTime != null">
                create_time = #{bean.createTime},
            </if>
            <if test="bean.status != null">
                status = #{bean.status},
            </if>
        </set>
        <include refid="query_condition"/>
    </update>

    <!--多条件删除-->
    <delete id="deleteByParam">
        delete from user_moments_comment
        <include refid="query_condition"/>
    </delete>

    <!-- 根据CommentId修改-->
    <update id="updateByCommentId" parameterType="com.easychat.entity.po.UserMomentsComment">
        UPDATE user_moments_comment
        <set>
            <if test="bean.momentsId != null">
                moments_id = #{bean.momentsId},
            </if>
            <if test="bean.userId != null">
                user_id = #{bean.userId},
            </if>
            <if test="bean.nickName != null">
                nick_name = #{bean.nickName},
            </if>
            <if test="bean.replyUserId != null">
                reply_user_id = #{bean.replyUserId},
            </if>
            <if test="bean.replyNickName != null">
                reply_nick_name = #{bean.replyNickName},
            </if>
            <if test="bean.content != null">
                content = #{bean.content},
            </if>
            <if test="bean.createTime != null">
                create_time = #{bean.createTime},
            </if>
            <if test="bean.status != null">
                status = #{bean.status},
            </if>
        </set>
        where comment_id=#{commentId}
    </update>

    <!-- 根据CommentId删除-->
    <delete id="deleteByCommentId">
        delete
        from user_moments_comment
        where comment_id = #{commentId}
    </delete>

    <!-- 根据PrimaryKey获取对象-->
    <select id="selectByCommentId" resultMap="base_result_map">
        select
        <include refid="base_column_list"/>
        from user_moments_comment where comment_id=#{commentId}
    </select>

</mapper>
